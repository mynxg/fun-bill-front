import * as configController from './configController';

/**
 * 暴露所有的控制器
 * @type {{configController: *}}
 * @description 暴露所有的控制器
 * @typedef controllers
 * @property {configController} configController - 配置控制器
 */
export default {
    configController,
};